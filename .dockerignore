run/
igate.secrets
*.md
.git/

# The web monitor runs on the HOST, not in the container: it shells out to
# deploy_igate.sh, which in docker mode shells out to docker.
igate_web.py
web/

# Per-machine configuration; the container reads none of it.
igate.local.conf
